# 🔧 Fix Database Schema Error

## Error: `column leads.user_id does not exist`

This means your Supabase database tables haven't been created yet.

## ✅ Solution: Run the SQL Schema

### Step 1: Open Supabase Dashboard
1. Go to https://supabase.com/dashboard
2. Select your project: **uyzzgohbtmieuvgdnicb**

### Step 2: Open SQL Editor
1. Click **SQL Editor** in the left sidebar (icon looks like `</>`)
2. Click **New Query** button

### Step 3: Copy and Run the Schema
1. Open the file `supabase-schema.sql` in this project
2. Copy ALL the SQL code (Ctrl+A, Ctrl+C)
3. Paste it into the Supabase SQL Editor
4. Click **Run** button (or press Ctrl+Enter)

### Step 4: Verify Tables Created
1. Click **Table Editor** in the left sidebar
2. You should see two tables:
   - ✅ `leads` (with columns: id, user_id, name, phone, email, company, status, source, created_at, updated_at)
   - ✅ `call_logs` (with columns: id, user_id, lead_id, lead_name,# 🔧 Fix Database Schema Error

## Error: `column leads.user_id dop 
## Error: `column leads.user_ito 
This means your Supabase database tables have(F5
## ✅ Solution: Run the SQL Schema
e! ✨

---

## What the Schema
### Step 1: Open Supabase Dashboa_lo1. Go to https://supabase.com/dashty2. Select your project: **uyzzgohbtmiea

### Step 2: Open SQL Editor
1. Click ce
- Sets up1. Click **SQL Editor** in

2. Click **New Query** button

### Step 3: Copy and Run the Schemaes
### Step 3: Copy and Run thes 1. Open txist (safe to ignore)
- Permission errors (make sure you're the project owner)
- S3. Paste it  (make sure you copied the ent4. Clile)

